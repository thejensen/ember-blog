{{#link-to 'post' post.id}}
  <div class="row">
    <div class="col-sm-8">
      <h4>{{post.title}}</h4>
      <p>{{post.content}}</p>
    </div>
    <div class="col-sm-4">
      <img src={{post.image}}>
    </div>
  </div>
{{/link-to}}
